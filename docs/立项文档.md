# 《像素分形水族箱》立项文档

| 项目名称 | 像素分形水族箱（Pixel Fractal Aquarium） |
| --- | --- |
| 项目类型 | 交互式数字生态 / 艺术与玩法融合的桌面应用（Web 端，Three.js） |
| 技术栈 | TypeScript + Three.js（WebGL2）+ GLSL Shader |
| 目标平台 | 桌面浏览器（Chrome / Edge / Firefox，优先 Chromium） |
| 版本 | v0.1（立项稿） |
| 状态 | 待评审 |

---

## 1. 项目概述

《像素分形水族箱》是一个将数学之美与数字生命融合的交互式桌面生态——曼德博罗集作为海底"基因图谱"动态编码珊瑚礁的形态，鱼群的实时游弋轨迹又反过来影响珊瑚的生长与色彩，形成一个"基因→骨骼→生命→反馈基因"的闭环生态；整个体验用像素风格的复古滤镜包裹，让用户在一个窗口中同时感受分形数学的无限细节、AI 生命体的灵动随机，以及亲手"培育"一个微型数字生态系统的治愈感。

**一句话定位**：一个"会生长的曼德博罗集"，一座用户可以亲手培育、观察并与鱼群共处的像素风数字水族箱。

---

## 2. 项目背景与动机

### 2.1 背景

- 分形数学（曼德博罗集）具有"无限细节、自相似、参数小变化引发形态剧变"的特性，天然适合作为程序化生成（Procedural Generation）的基因编码。
- Web 端实时渲染技术（Three.js / WebGL2 / Shader）已能承载逐像素分形计算与大规模实例化生物学模拟，无需客户端安装。
- 像素复古（Pixel Art / Retro）审美近年持续流行，与"分形的无限细节"形成有趣的张力：以有限的像素分辨率表现无限细节的数学对象。

### 2.2 动机

1. **数学可感化**：让分形数学从公式走向可交互、可养殖的生命体，降低理解门槛，提升趣味性。
2. **生态闭环叙事**：现有"虚拟水族箱/电子宠物"多为单向播放或简单成长数值；本项目强调**双向反馈**——鱼群行为影响环境，环境又反过来决定鱼群，形成可观察的生态叙事。
3. **治愈系体验**：低强度、无压力的"培育"玩法，契合当代家庭/办公场景下的减压需求。

---

## 3. 核心概念与世界观

项目将整个系统建模为一条完整的"生命闭环"：

```
基因（曼德博罗集） → 骨骼（珊瑚形态） → 生命（鱼群 AI） → 反馈（生态影响） → 回到基因
```

### 3.1 基因图谱：曼德博罗集

- 曼德博罗集的 Escape-time 迭代结果将作为**染色体纹理**（Fractal Genome Texture）存在。
- 用户可通过调整参数（`c` 偏移、迭代次数、`z` 初值、着色调色板）设计自己的"鱼缸基因"。
- 不同参数区域（Mandelbrot 的"海马谷""闪电区"等）映射为不同的珊瑚形态类型，构成遗传多样性。

### 3.2 骨骼：珊瑚形态生成

- 珊瑚的"骨骼"由**分形驱动的生长算法**生成：以基因组纹理为采样源，决定分支角度、生长方向、密度与高度。
- 生长算法（L-System 风格迭代 + 基于纹理的场引导）在**确定性生成（种子固定）**与**生态随机性**之间平衡。
- 珊瑚以 SDF（有符号距离场）或实例化几何（InstancedMesh）渲染，支持生长动画与用户修剪/播种交互。

### 3.3 生命：鱼群 AI

- 鱼群采用 **Boids 模型（分离 / 聚合 / 对齐）+ 感知-决策行为层**：
  - **生存层**：觅食、躲藏、游弋能量消耗。
  - **社交层**：跟随鱼群、回避大型障碍（珊瑚）。
  - **好奇层**：对用户光标、投放的"食物"/"光斑"做出反应。
- 每条鱼携带一个从基因组纹理采样的"个性基因"，影响速度、胆量、转向偏好，使每条鱼行为可辨识、可被"培育"。

### 3.4 反馈：生态闭环（核心创新点）

鱼群的游弋轨迹将**实时写入生态影响场**，反向作用于珊瑚：

- **滋养**：鱼群密集活动区域，珊瑚生长加速、色彩饱和度提升（分泌"养分"）。
- **重塑**：高强度 "curl" 轨迹（如大量鱼群绕行）会局部扰动珊瑚生长方向，形成新的分形弯曲细节。
- **色彩联动**：珊瑚调色板随鱼群"健康指数"（活动量、觅食成功率）在基因组提供的色域内漂移。
- **反向闭环**：珊瑚形态变化又改变鱼群的可通行空间，进而影响鱼群行为——生态进入自我调节。

> 体验目标：用户能在数分钟内观察到"种下珊瑚 → 鱼群繁衍 → 珊瑚因鱼群改变形态与色彩"的完整闭环，并可通过参数干预产生完全不同走向的鱼缸。

### 3.5 交互方式（桌面端）

| 交互 | 行为 |
| --- | --- |
| 鼠标移动 | 鱼群好奇跟随 / 垂直光柱引导 |
| 左键点击/拖拽 | 投放食物（吸引鱼群）、"播种"新珊瑚 |
| 右键 | 局部"溶解"或修剪珊瑚 |
| 滚轮 | 缩放视角（拉近观察珊瑚分形细节） |
| 面板 | 基因参数调节（c 值、调色板、迭代深度、生态速率）、存档/读档 |

---

## 4. 视觉与音效风格

### 4.1 像素复古滤镜（Retro/Pixel Filter）

- **渲染链**：场景先渲染至低分辨率离屏 RenderTarget（可调 `baseScale`，默认约 1/4~1/6），再以 `NearestFilter` 放大到屏幕，天然产生像素块。
- **色彩量化**：输出阶段应用有序抖动（Ordered/Bayer Dithering）将色深降至 16/32/64 色，强化 CRT 复古感。
- **可选增强**：扫描线（Scanline）、轻微色差（Chromatic Aberration）、CRT 曲面弯曲（Curvature），以开关形式提供，默认适度开启。
- **性能权衡**：低分辨率渲染同时是性能红利，允许容纳更大规模鱼群与更高分形迭代深度。

### 4.2 美学基准

- 主色调：深海蓝紫 + 荧光珊瑚色（根据基因组调色板变化）。
- 灵感参考：复古 16-bit/32-bit 像素海景、《Verdant Skies》《Endless Ocean》的治愈氛围 + 《Creatures》的基因-生态玩法。

### 4.3 音效（可选阶段）

- 环境低频海底白噪 + 气泡音；
- 鱼群密度驱动的轻量打击乐/琶音（密度越高，音符越密）；
- 交互反馈音（播种、投食、修剪）。

---

## 5. 技术方案

### 5.1 总体架构

```
┌─────────────────────────────────────────────────────┐
│                    UI 层（控制面板/存档）              │
├─────────────────────────────────────────────────────┤
│ 生态模拟内核（CPU）                                     │
│  · 鱼群 AI（Boids + 行为层）   · 生态影响场    · 生长器  │
├─────────────────────────────────────────────────────┤
│ 渲染与 GPU 管线（Three.js / WebGL2）                   │
│  · 分形 Shader（基因组纹理）  · 珊瑚实例化渲染          │
│  · 鱼群实例化渲染            · 后处理（像素滤镜栈）     │
└─────────────────────────────────────────────────────┘
```

### 5.2 分层技术栈

| 模块 | 选型与说明 |
| --- | --- |
| 语言/构建 | TypeScript + Vite |
| 3D 渲染 | **Three.js**（WebGL2；预留 WebGPU 迁移评估） |
| 分形计算 | GLSL Fragment Shader，离屏渲染生成/更新基因组纹理（可降级 CPU 计算） |
| 鱼群模拟 | CPU 侧 Boids（几千条量级）＋ InstancedMesh 渲染；量级上万时迁移 GPU Compute |
| 珊瑚生长 | 生长模拟器（CPU，低频 tick）＋ 实例化骨节 / SDF 混合碰撞 |
| 像素滤镜 | 自定义后处理 Pass（低分辨率 RT + Nearest 放大 + Bayer 抖动） |
| 状态持久化 | 鱼缸快照序列化（基因组参数、珊瑚结构、种群统计、生态场），localStorage / 导出文件 |
| 单元测试 | Vitest（生长算法、噪声/分形采样、Boids 参数化仿真） |

### 5.3 关键实现点

1. **基因组纹理（Genome Texture）**：`FractalPass` 以可编辑 uniform（`c`, `maxIter`, `zoom`）离屏计算曼德博罗集迭代计数，经着色映射为 RGBA 纹理；珊瑚生成、鱼类个性、调色板均从此纹理采样——与"一切源于基因"的世界观一致。
2. **生态影响场（Influence Field）**：一张低分辨率浮点纹理/二维数组（如 64×64），鱼群每帧对其写入"养分/扰动"；珊瑚生长器按场值调节生长速率与偏转。格点分辨率与性能可调。
3. **确定性 + 随机**：物种实例由 `(genomeSeed, 生态随机种子)` 决定，保证观感灵动且可复现存档。
4. **性能预算**：目标 60 FPS @ 1080p；实例化物为单位；后处理在低分辨率下进行；分形迭代控制在 64~256 档动态调节。

---

## 6. 里程碑计划

| 阶段 | 周期（估） | 目标与交付物 |
| --- | --- | --- |
| M0 技术验证 | 1 周 | Three.js 场景 + 曼德博罗集 Shader 纹理 + 像素后处理（Nearest + Bayer）跑通 |
| M1 生态内核 | 2 周 | Boids 鱼群（CPU）+ 珊瑚生成器（确定性）+ 生态影响场雏形 |
| M2 闭环串联 | 2 周 | 鱼群→影响场→珊瑚生长/色彩反馈打通；基础交互（投食/播种/缩放） |
| M3 体验打磨 | 2 周 | 像素滤镜增强（扫描线/色差）、调色板与基因组 UI、音效 |
| M4 存档与发布 | 1 周 | 存档/导出、性能调优、兼容性与打包发布（静态站点） |

> 总计约 8 周（单人全职估算；多人并行可压缩）。

---

## 7. 验收标准（MVP）

- [ ] 60 FPS 下同时运行 ≥ 3,000 条实例化鱼 + 1,000 珊瑚骨节（典型桌面 GPU）。
- [ ] 曼德博罗集参数可实时调节，且视觉变化可感知地映射到珊瑚形态。
- [ ] 鱼群游弋轨迹对珊瑚生长方向/色彩产生可观察影响（启用前后对比明显）。
- [ ] 像素复古滤镜完整应用（低分辨率、最近邻放大、颜色抖动），且可一键开关。
- [ ] 支持存档/读档，存档加载后生态（形态 + 种群）可复现。
- [ ] 用户可在 5 分钟内完成一次可见的生态闭环观察，无需阅读说明。

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 鱼群数量上升导致 CPU 模拟成为瓶颈 | 帧率下降 | Boids 使用空间哈希；必要时迁移 GPU Compute；默认规模控制 |
| 分形 Shader 拉高 GPU 负担（迭代深度 × 全屏） | 帧率下降 | 基因组纹理按需更新（参数变化/低频 tick），非每帧全量重算 |
| 生态闭环"反馈感"不明显 | 核心卖点失效 | 前置"显性反馈"（珊瑚闪烁/变色提示）；提供增益系数与对比模式 |
| 像素滤镜掩盖交互细节 | 可读性下降 | 提供滤镜强度/关闭选项；交互高亮层不参与降分辨率 |
| 存档结构随版本演进破坏兼容 | 存档损坏 | 存档带版本号与迁移函数 |

---

## 9. 团队与资源

- **角色**（预计 1–3 人可启动）：
  - 程序（渲染管线 / Three.js / Shader）—— 核心；
  - 程序（模拟算法：Boids、生长算法、生态场）—— 可与渲染一人兼任；
  - 设计/美术（像素美学、调色板、UI）与音效（均可外包或使用素材库）。
- **设备**：一台带独立 GPU 的开发机 + 若干测试机型（含核显笔记本）。
- **资产**：除音效素材外，美术资产全部程序化生成，零版权依赖。